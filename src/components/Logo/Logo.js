import React from 'react';

import classes from './Logo.module.scss';

const logo = (props) => (

	<div className={classes.logo}>
		<a href="#top" title="LeaderClasses Homepage">
			Leader<span>Classes</span>
		</a>
	</div>

);

export default logo;
